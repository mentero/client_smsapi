require 'spec_helper'

describe ClientSmsapi do
  it 'has a version number' do
    expect(ClientSmsapi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
